"use client";

import { useEffect } from 'react';

export default function VisualEditsMessenger() {
  useEffect(() => {
    // This component handles visual editing messages
    // For now, it's a placeholder that doesn't interfere with the app
    console.log('Visual edits messenger initialized');
  }, []);

  return null;
}